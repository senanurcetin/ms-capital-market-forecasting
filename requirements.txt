# What the APPLICATION needs to run: the dashboard, the API, and loading a model.
#
# Deliberately lean, and it is the root requirements.txt for a reason - Streamlit
# Community Cloud installs this file and nothing else, so anything here is something the
# published dashboard waits for on every rebuild. The heavy pipeline-only packages
# (mlflow, shap, duckdb, polars, kaggle) live in requirements-pipeline.txt.
pandas==2.2.3
numpy==2.4.6
pyarrow==24.0.0
scikit-learn==1.8.0
lightgbm==4.6.0
# xgboost-cpu, NOT xgboost: the default wheel pulls the NVIDIA CUDA runtime,
# measured at 454 MB, which nothing here uses - this project trains on CPU -
# and which Streamlit Cloud would download on every rebuild.
xgboost-cpu==3.2.0
joblib==1.5.3
pyyaml==6.0.3
fastapi==0.136.1
uvicorn==0.30.6
pydantic==2.13.4
streamlit==1.57.0
plotly==6.7.0
