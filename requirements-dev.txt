-r requirements-pipeline.txt
pytest==8.4.2
pytest-cov==7.0.0
ruff==0.14.4
httpx
# Hyperparameter search only - deliberately NOT in the runtime or pipeline sets, so
# neither the serving image nor the training image carries it.
optuna==4.9.0
