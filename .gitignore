__pycache__/
*.py[cod]
.venv/
venv/
.env
*.feather
*.parquet

# ...except the dashboard's exported bundle. results/ carries derived aggregates and one
# small feature sample, and it has to be IN the repository or the published dashboard
# renders empty pages - which looks like a broken app, not a missing file.
!results/*.parquet
data/raw/*
data/processed/*
data/features/*
!data/**/.gitkeep
mlruns/
mlartifacts/
.pytest_cache/
.ruff_cache/
*.log
secrets/

# Kimlik bilgileri - asla commit edilmez
*service-account*.json
*credentials*.json
kaggle.json
gcp-key.json
.env*
.demo/

# Tooling caches and coverage output
.coverage
htmlcov/
